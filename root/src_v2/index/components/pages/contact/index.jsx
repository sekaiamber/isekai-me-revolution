import React from 'react';
import subpage from '../subpage';

function Contact(props) {
  console.log(props);
  return (
    <div id="contact">
      contact
    </div>
  );
}

export default subpage(Contact);
